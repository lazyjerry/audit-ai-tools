# GitHub 倉庫資安掃描報告

╔══════════════════════════════════════════════════════╗
║        GitHub 倉庫資安掃描報告                      ║
╠══════════════════════════════════════════════════════╣
║  倉庫：terryso/claude-auto-resume                   ║
║  網址：https://github.com/terryso/claude-auto-resume ║
║  掃描時間：2026-04-04 15:14:11                      ║
╚══════════════════════════════════════════════════════╝

## 倉庫基本資訊

| 項目 | 值 |
|------|----|
| 類型 | 混合型：Claude 指示檔 + 通用 Shell 工具 |
| 說明 | 自動等待 Claude 額度恢復後續跑任務，或在等待結束後執行自訂 Shell 命令 |
| 星數 | 713 |
| 授權 | MIT License |
| 最後更新 | 2026-04-03T15:20:33Z |

## 偵測到的 AI 工具擴充

### Claude 指示檔
- 檔案：CLAUDE.md
- 用途摘要：提供 Claude Code 在此倉庫中的工作說明與安全警告。
- 安裝後影響：不會直接改寫使用者本機設定，但主腳本會在等待額度恢復後，以略過權限確認的方式自動呼叫 Claude CLI。

## 風險摘要

| 嚴重性 | 數量 |
|---------|------|
| Critical | 0 |
| High | 2 |
| Medium | 0 |
| Low | 0 |

## 詳細發現

### ⚠️ [High] G4-1 - 安裝流程包含高權限寫入與遠端內容直寫系統路徑
- 位置：README.md 安裝章節
- 內容：`wget -qO- https://raw.githubusercontent.com/terryso/claude-auto-resume/refs/heads/develop/claude-auto-resume.sh | sudo tee /usr/local/bin/claude-auto-resume >/dev/null && sudo chmod +x /usr/local/bin/claude-auto-resume`
- 說明：文件建議直接從遠端下載腳本並透過 `sudo` 寫入系統 PATH，也提供 `sudo make install`、`sudo cp`、`sudo ln -s` 等全域安裝方式。這不是隱藏惡意行為，但會放大供應鏈風險與提權面。

### ⚠️ [High] B3 - 預期用途但高風險的任意命令執行設計
- 位置：claude-auto-resume.sh
- 內容：`eval "$command"`
- 說明：工具以 `eval` 執行使用者提供的命令字串，允許管線、重導向與多重指令。這屬於產品設計，不是隱藏後門，但本質上就是完整 Shell 任意執行能力。

## 遙測與追蹤分析

### 本地紀錄行為
- 紀錄位置：未發現
- 紀錄格式：未發現
- 紀錄內容：未發現 `telemetry`、`analytics`、`installation_id`、`jsonl` 等持久追蹤或結構化使用紀錄
- 觸發條件：未發現預設啟用的本地行為紀錄機制

### 遠端上傳行為
- 上傳端點：未發現用於資料外傳的第三方端點
- 上傳內容：未發現主動上傳本地檔案內容、憑證或專案中繼資料的程式碼
- 匿名化程度：不適用
- Opt-out 機制：不適用

### 追蹤 ID
- 是否存在持久 ID：否
- 儲存位置：無
- 用途：未發現

## 安裝建議

- ⚠️ 謹慎考慮
- 理由：未發現隱藏式遙測或資料外傳，但工具核心能力就是自動化高權限操作，風險主要來自功能本身。
- 若決定安裝，建議注意事項：
  1. 只在隔離的開發環境使用，不要放在正式環境或含敏感憑證的工作目錄。
  2. 不要使用 README 內的遠端下載直寫方式，先審閱腳本內容，再以本地檔案安裝。
  3. 不要把不可信的 prompt 或命令字串交給此工具。
  4. 若只是想自動續跑 Claude，應優先考慮保留權限確認的模式；此專案目前主打的是跳過確認。

> ⚠️ 提醒：本報告由 AI 自動產生，結果可能存在誤判或遺漏，所有發現仍需人工核實與驗證，不應作為唯一的安全評估依據.
